const arr = ["hola", "adios", "prueba"];
const arrRandom = Math.floor(Math.random() * arr.length);

const obj = {
	anecdote1: {
		anecdote: "hola",
		vote: 0,
	},
};

// console.log(arr.length, arrRandom)
console.log(obj);
